# Android TODO

작성 2026-09-02. iOS 쪽은 `../dignify-iOS/TODO.md`, 백엔드는 `../dignify-backend/TODO.md`.

**현재 상태 — `versionName 1.1.0` / `versionCode 9`.** 하입 따라가기 피드까지 Play에 나가 있다.
1.1.0은 **코드 변경 없이 번호와 릴리즈 노트만 붙인 릴리즈**다(자세한 사정은 §P0).

**iOS와의 격차 = iOS 1.1.1 한 버전.** 항목별 지시는 `docs/1.1.1-port-checklist.md`가 정본이고,
여기엔 순서와 판단만 적는다.

---

## P0. 1.1.0 올리기 — 노트만 붙은 릴리즈 (코드 0줄)

버전과 노트는 2026-09-02에 이미 커밋했다. 남은 건 **올리는 것뿐**이다(`docs/release-runbook.md`).

**왜 코드 없이 올리나.** 하입 피드는 `versionCode 8`(`versionName 1.0.1`)로 이미 나갔는데
그때 `Changelog.kt`에 항목이 없었다. `shouldShow`가 노트 없는 버전을 걸러내므로
**그 업데이트를 받은 사람은 피드가 바뀐 걸 통보받지 못했다.** 1.1.0은 그걸 메우는 릴리즈다.

⚠️ **1.1.1 노트를 미리 적지 말 것.** 아래 포트가 안 끝났고, 안 만든 걸 새 소식이라 말하면 거짓말이 된다.

---

## P1. iOS 1.1.1 포트 — 온보딩 (가장 크고, 먼저 한다)

`SoundRoundsScreen.kt`를 **시드 풀에서 직접 고르는 한 화면으로 교체**한다. 남기는 게 아니라 지운다.
호출부 두 곳(`MainActivity.kt:186` 신규, `:315` 업데이트 유저) 다 갈아야 한다.

**서버는 이미 준비돼 있다** — `GET /onboarding/seed-pool`, 곡도 적재 완료(2026-09-02).

왜 바꾸나: 라운드는 18곡 고정 풀에서 6곡만 보여줬고, 그 6곡이 취향에 안 맞으면 시드를 고칠 길이
5단계였다. 실측으로 라운드 완주 33명 중 시드 저장까지 간 사람이 4명이다. 그 길이 죽어 있었다.

세부는 체크리스트 §2.1. 놓치면 안 되는 것 두 개만 여기 옮긴다.

- **아트워크 탭은 재생, 선택은 별도 배지.** 한 탭에 겹치면 소리가 아니라 아는 이름으로 고르게 되는데,
  그게 애초에 라운드를 만든 이유였다.
- **하입을 기다린 다음 완료를 알린다.** 던지고 넘어가면 첫 피드 요청이 하입보다 먼저 도착해서
  개인화가 안 걸린 피드를 본다.

---

## P2. iOS 1.1.1 포트 — 픽 작성화면 (리스트 + 날짜 + 재생)

`PickComposeSheet.kt`를 3열 그리드에서 **날짜별 리스트 + 행 탭 재생**으로 바꾼다.

작성화면을 연 53명 중 45명이 아무것도 못 내고 나갔고, 32명은 한 번 열어보고 재시도가 없었다.
그리드는 **이미 아는 곡**을 찾을 때 빠른데, 하입 목록은 대부분 기억나지 않는 곡이다.

- ⚠️ **`PickComposeSheet.kt:98` 주석이 그리드를 써야 하는 이유를 논증하고 있다.** 이번 변경이 그
  판단을 뒤집으니 주석도 같이 고칠 것. 안 그러면 다음 사람이 그리드를 되돌려놓고 회귀를 고쳤다고 여긴다.
- ⚠️ **선택 비교는 지금처럼 `trackId`로만 한다**(`:291`). iOS는 값 전체를 비교하다가 `hypedAt`을
  추가한 순간, 검색에서 고른 곡이 하입 목록에서 해제가 안 되고 **두 번 담기는** 버그가 났다.
  안드로이드엔 없는 버그다. `Candidate` 동등성으로 바꾸면서 새로 만들지 말 것.
- 날짜 묶기는 `HypeGrouping`을 재사용한다. 지금 `Api.HypeItem` 전용이라 iOS처럼 제네릭
  `byDay(items, date:)`를 빼고 `dayGroups`가 그걸 부르게 한다. 그루퍼를 두 개 만들면 두 목록의
  날짜 기준이 언젠가 갈린다.

---

## P3. iOS 1.1.1 포트 — 픽 카드 재생 수

`Api.Pick`에 `playCount` 추가하고 곡 수 옆에 그린다. **5 미만이면 숨긴다** — "3번 재생됨"은
창작자에게 0보다 나쁜 신호다.

서버는 `GET /picks/{id}`(상세 열기 = 재생 진입)에서만 올린다. 클라가 보낼 건 없다.
한국어는 `재생 %d회`다 — 픽을 열면 바로 재생이 시작되므로 조회수가 아니라 재생수가 맞다.

---

## P4. 문구 — `Your crate` → `Your hypes`

`R.string.your_crate`(3곳)와 `R.string.pick_crate_empty`(1곳). **한국어도 같이 옮긴다**
(`담은 곡` → `하입한 곡`). 영어만 바꾸면 유저 대부분이 읽는 언어에 두 이름 문제가 그대로 남는다.

crate는 오타가 아니라 앱 이름의 어원(crate digging)이다. 다만 네 곳뿐이라 학습될 기회가 없는 반면
"하입"은 어디에나 있다.

---

## P5. 계측 — iOS와 **한 글자도** 다르면 안 된다

PostHog 프로젝트가 하나라서, 이름이 한 글자 다르면 퍼널이 반으로 갈린다.
신규 9종 표는 체크리스트 §2.5에 있다. 라운드와 함께 은퇴하는 3종(`onboarding_sound_*`)은
보내기만 멈추고 과거 데이터는 그대로 둔다.

---

## P6. 푸시 딥링크 — **서버가 나왔다. 안드로이드는 분기가 하나도 없다**

2026-09-02에 서버가 `type`을 싣기 시작했다(`d06bd4f`). 값은 셋이다: `curation`, `pick_reaction`, `notice`.

**안드로이드는 `MainActivity.kt:135`에서 `push_opened`에 `type`을 실어 보내기만 하고
어떤 분기도 하지 않는다.** iOS는 `curation` 분기가 출시된 빌드에 이미 있어서 서버 배포만으로 살아났지만,
안드로이드는 **큐레이션 푸시도 그냥 앱만 열린다.** 두 개를 다 만들어야 한다.

| type | 가야 할 곳 |
|---|---|
| `curation` | 피드 탭 + 큐레이션 세트를 다시 앞세우기(iOS `pendingCurationOpen`과 같은 1회성 플래그) |
| `pick_reaction` | 마이페이지 → 디깅 프로필(= 내 픽 목록). **픽 탭이 아니다** — 내 픽이 목록 어디에 있는지 알 수 없다 |
| `notice` | 분기 없음. 갈 데를 지정하지 않은 알림이라 이게 의도한 동작이다 |

⚠️ **어느 픽인지는 아직 페이로드에 없다.** `pickId`를 서버가 실어주면 픽 상세로 바로 보낼 수 있다.
iOS `TODO.md` P6에 같은 요청이 걸려 있으니 **한쪽만 정하지 말고 같이 갈 것.**
